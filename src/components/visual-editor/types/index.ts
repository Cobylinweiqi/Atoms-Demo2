// ═══════════════════════════════════════════════════════════════
// 类型统一导出
// ═══════════════════════════════════════════════════════════════

export type {
  NodeType,
  SpacingBox,
  ShadowValue,
  ComponentStyles,
  ComponentNode,
} from "./node";

export type {
  FieldType,
  PropertyGroup,
  SelectOption,
  PropertyFieldSchema,
  ComponentSchema,
  Viewport,
} from "./schema";

export type { HistoryActionType, HistoryEntry } from "./history";
