# 05. 目录结构

> **Nova Studio — AI Full Stack Builder**  
> 版本: v1.0 | 日期: 2026-07-16

---

## 5.1 Monorepo 根结构

```
nova-studio/
├── apps/                          # 应用层
├── services/                      # 微服务层
├── packages/                      # 共享包层
├── docker-compose.yml             # 开发环境编排
├── turbo.json                     # Turborepo 配置
├── pnpm-workspace.yaml            # pnpm 工作区配置
├── package.json                   # 根 package.json
├── .env.example                   # 环境变量示例
├── .gitignore
└── README.md
```

---

## 5.2 apps/web — Next.js 主应用

```
apps/web/
├── src/
│   ├── app/                        # App Router 路由
│   │   ├── (marketing)/           # 营销页面组（公开）
│   │   │   ├── page.tsx           # 首页
│   │   │   ├── pricing/page.tsx
│   │   │   ├── templates/page.tsx
│   │   │   ├── showcase/page.tsx
│   │   │   └── docs/page.tsx
│   │   │
│   │   ├── (auth)/                 # 认证页面组
│   │   │   ├── login/page.tsx
│   │   │   ├── register/page.tsx
│   │   │   └── forgot-password/page.tsx
│   │   │
│   │   ├── (app)/                  # 认证后应用
│   │   │   ├── layout.tsx         # 应用 Shell
│   │   │   ├── page.tsx           # 控制台首页
│   │   │   │
│   │   │   ├── [orgSlug]/
│   │   │   │   ├── layout.tsx      # 组织 Shell
│   │   │   │   ├── page.tsx        # 组织控制台
│   │   │   │   ├── projects/
│   │   │   │   │   ├── page.tsx    # 项目列表
│   │   │   │   │   └── new/page.tsx
│   │   │   │   ├── settings/page.tsx
│   │   │   │   ├── members/page.tsx
│   │   │   │   ├── billing/page.tsx
│   │   │   │   └── activity/page.tsx
│   │   │   │
│   │   │   ├── [orgSlug]/p/[projectSlug]/
│   │   │   │   ├── layout.tsx     # 工作区 Shell
│   │   │   │   ├── page.tsx       # Chat 标签
│   │   │   │   ├── code/page.tsx
│   │   │   │   ├── visual/page.tsx
│   │   │   │   ├── preview/page.tsx
│   │   │   │   ├── theme/page.tsx
│   │   │   │   ├── components/page.tsx
│   │   │   │   ├── agents/page.tsx
│   │   │   │   ├── deploy/page.tsx
│   │   │   │   ├── github/page.tsx
│   │   │   │   ├── deployments/page.tsx
│   │   │   │   └── settings/page.tsx
│   │   │   │
│   │   │   └── settings/           # 用户设置
│   │   │       ├── page.tsx
│   │   │       ├── api-keys/page.tsx
│   │   │       ├── preferences/page.tsx
│   │   │       ├── notifications/page.tsx
│   │   │       └── appearance/page.tsx
│   │   │
│   │   └── api/                    # BFF API 路由
│   │       ├── auth/[...nextauth]/route.ts
│   │       ├── ai/stream/route.ts  # SSE 流式代理
│   │       └── webhooks/
│   │           └── stripe/route.ts
│   │
│   ├── components/                 # 应用组件
│   │   ├── chat/                  # 聊天组件
│   │   ├── editor/                # 代码编辑器
│   │   ├── visual/                # 可视化编辑器
│   │   ├── preview/               # 预览组件
│   │   ├── theme/                 # 主题编辑器
│   │   ├── workspace/             # 工作区组件
│   │   ├── deploy/                # 部署组件
│   │   ├── agents/                # Agent 编排
│   │   ├── github/                # GitHub 同步
│   │   ├── team/                  # 团队管理
│   │   └── shared/                # 通用组件
│   │
│   ├── hooks/                      # 自定义 Hooks
│   ├── stores/                     # Zustand stores
│   ├── lib/                        # 工具函数
│   ├── styles/                    # 全局样式
│   └── types/                     # 类型定义
│
├── public/
├── next.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

---

## 5.3 apps/api — NestJS API 服务

```
apps/api/
├── src/
│   ├── main.ts                     # 入口
│   ├── app.module.ts               # 根模块
│   │
│   ├── modules/                    # 功能模块
│   │   ├── auth/                   # 认证
│   │   ├── user/                   # 用户
│   │   ├── organization/           # 组织
│   │   ├── project/                # 项目
│   │   ├── file/                   # 文件
│   │   ├── conversation/          # 对话
│   │   ├── ai/                    # AI 模型
│   │   ├── component/             # 组件
│   │   ├── theme/                 # 主题
│   │   ├── agent/                 # Agent
│   │   ├── deploy/                # 部署
│   │   ├── git/                   # GitHub
│   │   ├── preview/               # 预览
│   │   ├── billing/               # 账单
│   │   ├── notification/          # 通知
│   │   └── analytics/             # 分析
│   │
│   ├── common/                    # 公共模块
│   │   ├── guards/                # 认证守卫
│   │   ├── interceptors/          # 拦截器
│   │   ├── filters/              # 异常过滤器
│   │   ├── decorators/           # 自定义装饰器
│   │   └── pipes/                # 管道
│   │
│   ├── config/                    # 配置
│   └── database/                 # Prisma 接入
│
├── test/
└── tsconfig.json
```

---

## 5.4 services/ — 微服务层

### services/ai-orchestrator — AI 编排服务

```
services/ai-orchestrator/
├── src/
│   ├── providers/                  # LLM 提供商
│   │   ├── base.provider.ts       # 抽象基类
│   │   ├── openai.provider.ts
│   │   ├── anthropic.provider.ts
│   │   ├── google.provider.ts
│   │   ├── groq.provider.ts
│   │   └── ollama.provider.ts
│   │
│   ├── tools/                     # AI 工具定义
│   │   ├── file.tools.ts
│   │   ├── component.tools.ts
│   │   ├── theme.tools.ts
│   │   ├── preview.tools.ts
│   │   ├── deploy.tools.ts
│   │   └── index.ts
│   │
│   ├── prompts/                   # 系统提示词
│   │   ├── system.prompt.ts
│   │   ├── project.context.ts
│   │   └── templates/
│   │
│   ├── context/                   # 上下文构建
│   │   ├── builder.ts
│   │   └── compressor.ts
│   │
│   ├── streaming/                 # 流式引擎
│   │   ├── sse.handler.ts
│   │   └── tool.executor.ts
│   │
│   └── index.ts
└── tsconfig.json
```

### services/sandbox — 沙箱/预览服务

```
services/sandbox/
├── src/
│   ├── runner/                    # 容器运行器
│   │   ├── docker.runner.ts
│   │   └── webcontainer.runner.ts
│   ├── bundler/                   # 代码打包
│   │   ├── vite.config.ts
│   │   └── dependency.installer.ts
│   ├── preview/                   # 预览服务器
│   │   ├── server.ts
│   │   └── hmr.ts
│   └── index.ts
├── Dockerfile
└── tsconfig.json
```

### services/deploy — 部署服务

```
services/deploy/
├── src/
│   ├── providers/                  # 部署平台
│   │   ├── vercel.provider.ts
│   │   ├── netlify.provider.ts
│   │   └── docker.provider.ts
│   ├── builder/                   # 构建编排
│   │   ├── build.pipeline.ts
│   │   └── artifact.manager.ts
│   ├── domain/                    # 域名管理
│   │   └── dns.manager.ts
│   └── index.ts
├── Dockerfile
└── tsconfig.json
```

---

## 5.5 packages/ — 共享包层

### packages/shared — 共享类型与常量

```
packages/shared/
├── src/
│   ├── types/                      # TypeScript 类型
│   │   ├── user.types.ts
│   │   ├── project.types.ts
│   │   ├── file.types.ts
│   │   ├── message.types.ts
│   │   ├── component.types.ts
│   │   ├── theme.types.ts
│   │   ├── agent.types.ts
│   │   ├── deploy.types.ts
│   │   └── index.ts
│   ├── constants/                  # 常量
│   │   ├── models.ts
│   │   ├── plans.ts
│   │   └── index.ts
│   ├── schemas/                    # Zod 验证 schema
│   │   ├── project.schema.ts
│   │   ├── file.schema.ts
│   │   └── index.ts
│   └── index.ts
└── tsconfig.json
```

### packages/ui — 共享 UI 组件库

```
packages/ui/
├── src/
│   ├── components/                 # shadcn/ui 基础 (30+ 组件)
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── dialog.tsx
│   │   ├── dropdown-menu.tsx
│   │   └── ...
│   ├── primitives/                # 原子组件
│   │   ├── icon.tsx
│   │   └── spinner.tsx
│   ├── layouts/                   # 布局组件
│   └── index.ts
└── tsconfig.json
```

### packages/database — Prisma & 仓储层

```
packages/database/
├── prisma/
│   ├── schema.prisma              # 完整数据模型
│   ├── migrations/               # 迁移文件
│   └── seed.ts                   # 种子数据
├── src/
│   ├── client.ts                 # PrismaClient 单例
│   ├── repositories/             # 仓储模式
│   │   ├── user.repo.ts
│   │   ├── project.repo.ts
│   │   ├── file.repo.ts
│   │   ├── conversation.repo.ts
│   │   └── ...
│   └── index.ts
└── tsconfig.json
```

### packages/ai — AI SDK 封装

```
packages/ai/
├── src/
│   ├── sdk/                       # 统一 AI SDK
│   │   ├── client.ts             # 统一客户端
│   │   ├── types.ts              # 接口类型
│   │   └── router.ts            # 模型路由
│   ├── tools/                     # 工具定义与执行
│   │   ├── registry.ts
│   │   ├── definitions/
│   │   └── executor.ts
│   ├── prompts/                   # 提示词模板
│   │   ├── system.ts
│   │   ├── project-types/
│   │   │   ├── website.ts
│   │   │   ├── saas.ts
│   │   │   ├── dashboard.ts
│   │   │   └── ...
│   │   └── index.ts
│   ├── context/                   # 上下文管理
│   │   ├── builder.ts
│   │   ├── compressor.ts
│   │   └── token-counter.ts
│   └── index.ts
└── tsconfig.json
```

### packages/auth — 认证工具

```
packages/auth/
├── src/
│   ├── jwt.ts                    # JWT 工具
│   ├── oauth.ts                  # OAuth 工具
│   ├── permissions.ts            # 权限定义
│   └── index.ts
└── tsconfig.json
```

### packages/config — 共享配置

```
packages/config/
├── src/
│   ├── env.ts                    # 环境变量验证
│   ├── features.ts               # 功能开关
│   └── index.ts
└── tsconfig.json
```

### packages/eslint-config — ESLint 配置

```
packages/eslint-config/
├── base.js                       # 基础规则
├── next.js                       # Next.js 规则
├── nestjs.js                     # NestJS 规则
└── index.js
```

---

## 5.6 根配置文件

### turbo.json

```json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"]
    }
  }
}
```

### pnpm-workspace.yaml

```yaml
packages:
  - "apps/*"
  - "services/*"
  - "packages/*"
```

### docker-compose.yml（开发环境）

```yaml
version: "3.9"
services:
  postgres:
    image: postgres:16-alpine
    ports: ["5432:5432"]
    environment:
      POSTGRES_DB: nova_studio
      POSTGRES_USER: nova
      POSTGRES_PASSWORD: nova_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio
    ports: ["9000:9000", "9001:9001"]
    environment:
      MINIO_ROOT_USER: nova
      MINIO_ROOT_PASSWORD: nova_dev
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  redis_data:
  minio_data:
```

---

[← 上一章: 系统架构](./04-系统架构.md) | [返回索引](./README.md) | [下一章: 数据库设计 →](./06-数据库设计.md)
